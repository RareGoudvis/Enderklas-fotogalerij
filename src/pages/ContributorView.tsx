import { SchoolHeader } from '../components/SchoolHeader';

// Leerkracht-view (/uploaden) — brief §4. Album aanmaken + uploaden in eigen
// klas(sen). Volgt in Sprint 3 (albums) en Sprint 4 (upload).
export default function ContributorView() {
  return (
    <div className="min-h-full">
      <SchoolHeader subtitle="Uploaden" />
      <main className="mx-auto max-w-3xl px-4 py-8">
        <p className="text-ink/70">Uploadweergave — nog in opbouw.</p>
      </main>
    </div>
  );
}
